% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_analysis_unsexed.R
\name{run_analysis_unsexed}
\alias{run_analysis_unsexed}
\title{Estimate parental contributions using parental genotypes without known sex}
\usage{
run_analysis_unsexed(
  geno_parents_file = NULL,
  pheno_parents_file = NULL,
  geno_off_file = NULL,
  pheno_off_file = NULL,
  af_pool_file = NULL,
  out_dir = ".",
  maxgen = 1e+05,
  nrep = 5,
  popsize_factor = 10,
  use_example = FALSE
)
}
\arguments{
\item{geno_parents_file}{Path to the parental genotype file (row names = parent IDs, column names = SNP IDs).}

\item{geno_off_file}{Path to the offspring genotype file (same SNPs and order as in parent file).}

\item{pheno_off_file}{Path to offspring metadata file, must include `ID` and `pool` columns.}

\item{af_pool_file}{Path to observed allele frequency file (single column, no header).}

\item{out_dir}{Output directory to save result files (default = ".").}

\item{maxgen}{Maximum number of generations for DEoptim (default = 100000).}

\item{nrep}{Number of replicates to run (default = 5).}

\item{popsize_factor}{Multiplier for DEoptim population size (default = 10).}

\item{use_example}{Logical. If TRUE, use built-in example files.}
}
\value{
Saves output files to the `out_dir` directory, including estimated allele frequencies and combined parent contributions.
}
\description{
This function estimates parental contributions when the sex of the parents is unknown. It sums contributions across all combinations
of candidate sires and dams and merges them by parent ID.
}
\examples{
# Example using user input
run_analysis_unsexed(
  geno_parents_file = "geno_parents.txt",
  geno_off_file = "geno_off.txt",
  pheno_off_file = "pheno_off.txt",
  af_pool_file = "af_pool.txt",
  out_dir = "output"
)
}
